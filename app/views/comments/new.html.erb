<h1>New Comment</h1>

<%= @comment.errors.full_messages.join(", ") %>
<%= bootstrap_form_for @comment do |f| %>
  <div>
    <%#= f.label :body %>
    <%= f.text_area :body %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>
