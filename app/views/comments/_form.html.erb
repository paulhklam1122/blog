<%= @comment.errors.full_messages.join(", ") %>
<%= simple_form_for [@post, @comment], remote: true do |f| %>
<div class="text-left">
  <%#= f.text_area :body %>
  <%= f.input :body %>
  <%#= f.submit class: "btn btn-large btn-primary", remote: true %>
</div>
<%= f.button :submit, class: "btn-primary" %>
<% end %>
